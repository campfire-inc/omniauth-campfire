source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in omniauth-campfire.gemspec
gemspec


gem 'rake'

group :test do
  gem 'rspec', '~> 3.2'
  gem 'rack-test'
  gem 'simplecov'
  gem 'webmock'
end
